type CartAsProduct {
  id: ID
  cartId: Cart
  productId: Product
  quantity: Int
  total: Int
}

type Success {
  success: Boolean!
  message: String
}

input AddCartInput {
  cartId: String!
  productId: String!
  quantity: Int!
  total: Int!
}